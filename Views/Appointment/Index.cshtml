@model IEnumerable<AppointmentApplication.Models.Item>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Borrow Items List</h2>
        </div>
        <div class="col-6">
            Create Item
        </div>
    </div>
    @if(Model.Count() > 0)
            {

            }
</div>

